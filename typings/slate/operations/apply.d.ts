/**
 * Apply an `operation` to a `state`.
 *
 * @param {State} state
 * @param {Object} operation
 * @return {State} state
 */
declare function applyOperation(state: any, operation: any): any;
export default applyOperation;
