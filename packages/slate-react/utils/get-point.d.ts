/**
 * Get a point from a native selection's DOM `element` and `offset`.
 *
 * @param {Element} element
 * @param {Number} offset
 * @param {State} state
 * @param {Editor} editor
 * @return {Object}
 */
declare function getPoint(element: any, offset: any, state: any, editor: any): any;
export default getPoint;
