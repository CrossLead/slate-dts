/**
 * Scroll the current selection's focus point into view if needed.
 *
 * @param {Selection} selection
 */
declare function scrollToSelection(selection: any): void;
export default scrollToSelection;
