/**
 * Convert an <img> source `url` to a data URI and `callback(err, uri)`.
 *
 * @param {String} url
 * @param {Function} callback
 */
declare function srcToDataUri(url: any, callback: any): number;
export default srcToDataUri;
